# Environment Notes

Record local environment information here.

```bash
sw_vers
uname -m
xcodebuild -version
xcode-select -p
python3 --version
uv --version
git --version
```

## Useful Paths

```text
~/Documents/learning/coreai-models
~/Documents/learning/coreai-exports
~/.cache/huggingface
~/.cache/uv
```

## Disk Usage Checks

```bash
df -h
du -sh ~/.cache/huggingface 2>/dev/null
du -sh ~/.cache/uv 2>/dev/null
du -sh ~/Documents/learning/coreai-exports 2>/dev/null
```
